import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'panelControl',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
